- @articles.each do |article|
  = render 'article', article: article
- if user_signed_in?
	= button_to "Создать статью", new_article_path, class: "", method: :get
.row
  center
    = paginate @articles